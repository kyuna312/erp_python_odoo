<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="pay_reciept_items_search_wizard_form" model="ir.ui.view">
            <field name="name">pay.receipt.search.items.wizard.form</field>
            <field name="model">pay.receipt.search.items.wizard</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
<!--                            <field name="show_all"/>-->
<!--                            <field name="duureg_id" attrs="{'required': [('show_all','=',False)], 'invisible': [('show_all', '=', True)]}"/>-->
<!--                            <field name="horoo_id" attrs="{'required': [('show_all','=',False)], 'invisible': [('show_all', '=', True)]}"/>-->
<!--                            <field name="inspector_ids" widget="many2many_tags"/>-->
                            <field name="apartment_ids" widget="many2many_tags" options="{'no_create': True}"/>
                        </group>
                    </sheet>
                    <footer>
                        <button name="search_items" string="Хайх" type="object" class="btn-primary"/>
                        <button string="Цуцлах" class="btn-secondary" special="cancel" />
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>